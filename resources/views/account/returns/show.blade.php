@extends(auth()->user()->role === 'admin' ? 'layouts.admin' : 'layouts.account')
@section('title', 'Retour '.$return->number)
@section(auth()->user()->role === 'admin' ? 'admin-content' : 'account-content')
<a href="{{ route('account.order', $return->order) }}" class="text-secondary text-decoration-none"><i class="bi bi-arrow-left"></i> Commande {{ $return->order->number }}</a><div class="d-flex justify-content-between align-items-start my-3"><div><h1 class="section-title mb-1">{{ $return->number }}</h1><p class="text-secondary">Créé le {{ $return->created_at->translatedFormat('j F Y') }}</p></div><span class="badge text-bg-warning fs-6">{{ $return->status }}</span></div><div class="row g-4"><div class="col-lg-7"><div class="stat-card"><h2 class="h5 fw-bold">Articles retournés</h2>@foreach($return->items as $item)<div class="d-flex justify-content-between py-3 border-bottom"><span>{{ $item->orderItem->name }}</span><strong>× {{ $item->quantity }}</strong></div>@endforeach</div></div><div class="col-lg-5"><div class="stat-card"><h2 class="h5 fw-bold">Détails</h2><dl><dt>Motif</dt><dd>{{ $return->reason }}</dd><dt>Solution demandée</dt><dd>{{ $return->solution }}</dd><dt>Mode de retour</dt><dd>{{ $return->return_method }}</dd></dl>@if($return->admin_notes)<div class="alert alert-info mb-0">{{ $return->admin_notes }}</div>@endif</div></div></div>
@endsection
