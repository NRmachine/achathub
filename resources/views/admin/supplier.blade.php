@extends('layouts.admin')
@section('title','Agent fournisseur | AchatHub')
@section('admin-content')
<div class="admin-page-heading"><div><small>APPROVISIONNEMENT</small><h1>Agent fournisseur</h1><p>Parcours du catalogue LCD Phone dans son ordre réel, puis contrôle automatique des stocks.</p></div><div class="admin-heading-actions d-flex flex-column flex-sm-row gap-2"><form method="post" action="{{ route('admin.supplier.catalog.refresh') }}">@csrf<button class="btn btn-outline-dark"><i class="bi bi-diagram-3 me-1"></i> Lire l’arborescence</button></form><form method="post" action="{{ route('admin.supplier.sync') }}">@csrf<button class="btn btn-dark"><i class="bi bi-arrow-repeat me-1"></i> Contrôler les stocks</button></form></div></div>

<div class="row g-3 mb-4">
    <div class="col-6 col-xl-2"><div class="admin-kpi"><span>Variantes suivies</span><strong>{{ number_format($stats['variants'],0,',',' ') }}</strong><small class="text-secondary">Références fournisseur</small></div></div>
    <div class="col-6 col-xl-2"><div class="admin-kpi"><span>Chemins LCD</span><strong>{{ number_format($stats['catalog_nodes'],0,',',' ') }}</strong><small class="text-secondary">Arbre fournisseur</small></div></div>
    <div class="col-6 col-xl-2"><div class="admin-kpi"><span>Chemins terminés</span><strong>{{ number_format($stats['catalog_complete'],0,',',' ') }}</strong><small class="text-secondary">Pages parcourues</small></div></div>
    <div class="col-6 col-xl-2"><div class="admin-kpi"><span>En attente</span><strong>{{ number_format($stats['catalog_pending'],0,',',' ') }}</strong><small class="text-secondary">Reprise automatique</small></div></div>
    <div class="col-6 col-xl-2"><div class="admin-kpi"><span>Correspondances</span><strong>{{ number_format($stats['mapped'],0,',',' ') }}</strong><small class="text-secondary">Reliées à AchatHub</small></div></div>
    <div class="col-6 col-xl-2"><div class="admin-kpi"><span>Ruptures</span><strong>{{ number_format($stats['out'],0,',',' ') }}</strong><small class="text-secondary">À surveiller</small></div></div>
</div>

<div class="row g-4 mb-4">
    <div class="col-xl-7"><section class="admin-surface h-100"><div class="d-flex align-items-start gap-3 mb-3"><span class="supplier-icon"><i class="bi bi-signpost-split"></i></span><div><h2 class="h5 mb-1">Parcourir un chemin exact</h2><p class="text-secondary mb-0">Laissez le chemin vide pour continuer automatiquement, ou indiquez un chemin LCD Phone précis.</p></div></div><form class="row g-3" method="post" action="{{ route('admin.supplier.catalog.crawl') }}">@csrf<div class="col-12"><label class="form-label">Chemin fournisseur</label><input class="form-control" name="path" value="{{ old('path','Pièces Détachées > Apple > iPhone > iPhone 11') }}" placeholder="Pièces Détachées > Apple > iPhone > iPhone 11"></div><div class="col-sm-3"><label class="form-label">Chemins</label><input class="form-control" type="number" min="1" max="10" name="nodes" value="{{ old('nodes',1) }}" required></div><div class="col-sm-3"><label class="form-label">Pages</label><input class="form-control" type="number" min="1" max="5" name="pages" value="{{ old('pages',2) }}" required></div><div class="col-sm-3"><label class="form-label">Fiches max.</label><input class="form-control" type="number" min="1" max="100" name="products" value="{{ old('products',100) }}" required></div><div class="col-sm-3 d-flex align-items-end"><button class="btn btn-ah w-100"><i class="bi bi-play-fill me-1"></i> Démarrer</button></div></form></section></div>
    <div class="col-xl-5"><section class="admin-surface h-100"><h2 class="h5">Classement fiable</h2><div class="supplier-rule"><i class="bi bi-diagram-3"></i><span><strong>Chemin officiel</strong><small>Catégorie, marque, appareil et modèle viennent directement de LCD Phone.</small></span></div><div class="supplier-rule"><i class="bi bi-phone"></i><span><strong>Variante au bon modèle</strong><small>Une variante iPhone 13 n’est jamais rangée dans le chemin iPhone 11.</small></span></div><div class="supplier-rule"><i class="bi bi-clock-history"></i><span><strong>Reprise automatique</strong><small>La page et la prochaine fiche sont conservées après chaque passage.</small></span></div></section></div>
</div>

<section class="admin-surface mb-4">
    <div class="d-flex flex-wrap justify-content-between gap-2 mb-3"><div><h2 class="h5 mb-1">Progression par chemin</h2><p class="text-secondary small mb-0">Chaque ligne correspond à une branche réelle du catalogue fournisseur.</p></div></div>
    <div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Chemin LCD Phone</th><th>État</th><th>Progression</th><th>Dernier passage</th></tr></thead><tbody>
    @forelse($catalogNodes as $node)<tr><td><strong>{{ $node->pathLabel() }}</strong><small class="d-block text-secondary">ID LCD {{ $node->supplier_category_id }}</small></td><td><span class="badge {{ $node->crawl_status === 'complete' ? 'text-bg-success' : ($node->crawl_status === 'crawling' ? 'text-bg-warning' : ($node->last_error ? 'text-bg-danger' : 'text-bg-light')) }}">{{ ['pending'=>'En attente','partial'=>'À continuer','crawling'=>'En cours','complete'=>'Terminé'][$node->crawl_status] ?? ucfirst($node->crawl_status) }}</span></td><td>Page {{ $node->next_page }}{{ $node->max_page ? ' / '.$node->max_page : '' }}<small class="d-block text-secondary">{{ $node->products_seen }} fiche(s), {{ $node->variants_seen }} variante(s)</small></td><td>{{ $node->last_crawled_at?->diffForHumans() ?: 'Pas encore parcouru' }}@if($node->last_error)<small class="d-block text-danger">{{ str($node->last_error)->limit(90) }}</small>@endif</td></tr>@empty<tr><td colspan="4" class="text-center py-4">Cliquez sur « Lire l’arborescence » pour importer les chemins LCD Phone.</td></tr>@endforelse
    </tbody></table></div>
</section>

<section class="admin-surface mb-4">
    <div class="d-flex flex-wrap justify-content-between gap-2 mb-3"><div><h2 class="h5 mb-1">Références fournisseur</h2><p class="text-secondary small mb-0">Associez une référence au SKU AchatHub, puis activez la mise à jour automatique du stock.</p></div></div>
    <form class="admin-filter" method="get"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Nom ou référence fournisseur"><select class="form-select" name="status"><option value="">Tous les états</option><option value="ready" @selected(request('status')==='ready')>Prêts à importer</option><option value="mapped" @selected(request('status')==='mapped')>Associés</option><option value="unmapped" @selected(request('status')==='unmapped')>À associer</option><option value="syncing" @selected(request('status')==='syncing')>Synchronisation active</option><option value="out" @selected(request('status')==='out')>En rupture</option></select><button class="btn btn-dark">Filtrer</button></form>
    <div class="table-responsive"><table class="table align-middle supplier-table"><thead><tr><th>Produit fournisseur</th><th>Référence / achat</th><th>Stock fournisseur</th><th>Correspondance AchatHub</th></tr></thead><tbody>
    @forelse($products as $supplierProduct)
        <tr><td><div class="d-flex align-items-center gap-3">@if($supplierProduct->image)<img class="supplier-thumb" src="{{ $supplierProduct->image }}" alt="">@else<span class="supplier-thumb supplier-thumb-empty"><i class="bi bi-box"></i></span>@endif<div><strong class="d-block">{{ $supplierProduct->name }}</strong>@if($supplierProduct->variant_name)<small class="text-secondary d-block text-clamp">{{ $supplierProduct->variant_name }}</small>@endif @if($supplierProduct->supplier_path)<span class="badge text-bg-light mt-1 text-wrap text-start">{{ implode(' › ', $supplierProduct->supplier_path) }}</span>@if($supplierProduct->catalog_nodes_count > 1)<small class="d-block text-secondary mt-1">Compatible avec {{ $supplierProduct->catalog_nodes_count }} chemins</small>@endif @elseif($supplierProduct->suggestedCategory)<span class="badge text-bg-light mt-1">{{ $supplierProduct->suggestedCategory->parent?->name }} › {{ $supplierProduct->suggestedCategory->name }}</span>@endif<a class="small d-block mt-1" href="{{ $supplierProduct->supplier_url }}" target="_blank" rel="noopener">Voir chez le fournisseur <i class="bi bi-box-arrow-up-right"></i></a></div></div></td><td><code>{{ $supplierProduct->supplier_reference ?: 'Sans référence' }}</code><small class="d-block text-secondary mt-1">{{ $supplierProduct->purchase_price !== null ? number_format((float)$supplierProduct->purchase_price,2,',',' ').' € TTC / unité' : 'Prix indisponible' }}</small><small class="d-block text-secondary">Minimum {{ number_format($supplierProduct->minimum_order_quantity,0,',',' ') }}</small></td><td>@if($supplierProduct->supplier_stock>0)<span class="badge text-bg-success">{{ number_format($supplierProduct->supplier_stock,0,',',' ') }} brut</span><small class="d-block mt-1"><strong>{{ number_format($supplierProduct->sellableStock(),0,',',' ') }}</strong> vendable(s)</small>@else<span class="badge text-bg-danger">Rupture</span>@endif<small class="d-block text-secondary mt-2">{{ $supplierProduct->last_synced_at?->diffForHumans() ?: 'Jamais contrôlé' }}</small>@if($supplierProduct->last_error)<small class="d-block text-danger mt-1">{{ str($supplierProduct->last_error)->limit(80) }}</small>@endif</td><td>@if(!$supplierProduct->product_id)<a class="btn btn-sm btn-ah w-100 mb-2" href="{{ route('admin.supplier.import',$supplierProduct) }}"><i class="bi bi-plus-circle me-1"></i> Importer dans la boutique</a>@else<a class="btn btn-sm btn-outline-dark w-100 mb-2" href="{{ route('admin.products.edit',$supplierProduct->product) }}"><i class="bi bi-pencil me-1"></i> Modifier le produit</a>@endif<form class="supplier-map-form" method="post" action="{{ route('admin.supplier.update',$supplierProduct) }}">@csrf @method('patch')<label class="form-label small">SKU de la boutique</label><input class="form-control form-control-sm" name="product_sku" value="{{ $supplierProduct->product?->sku }}" placeholder="{{ $supplierProduct->suggestedProduct ? 'Suggestion : '.$supplierProduct->suggestedProduct->sku : 'Ex. AH-001' }}"><label class="form-label small mt-2">Unités fournisseur par vente</label><input class="form-control form-control-sm" type="number" min="1" max="100000" name="stock_divisor" value="{{ $supplierProduct->stock_divisor }}" required><div class="d-flex flex-wrap gap-3 my-2"><div class="form-check form-switch"><input class="form-check-input" type="checkbox" name="sync_stock" value="1" id="sync-{{ $supplierProduct->id }}" @checked($supplierProduct->sync_stock)><label class="form-check-label small" for="sync-{{ $supplierProduct->id }}">Synchroniser</label></div><div class="form-check form-switch"><input class="form-check-input" type="checkbox" name="active" value="1" id="active-{{ $supplierProduct->id }}" @checked($supplierProduct->active)><label class="form-check-label small" for="active-{{ $supplierProduct->id }}">Surveiller</label></div></div><button class="btn btn-sm btn-outline-dark w-100">Enregistrer le lien</button></form></td></tr>
    @empty<tr><td colspan="4" class="text-center py-5"><i class="bi bi-inboxes fs-2 d-block mb-2"></i>Aucune référence. Lancez une première découverte limitée.</td></tr>@endforelse
    </tbody></table></div>{{ $products->links() }}
</section>

<div class="row g-4"><div class="col-xl-8"><section class="admin-surface h-100"><h2 class="h5 mb-3">Historique de l’agent</h2><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Démarrage</th><th>Action</th><th>État</th><th>Résultat</th><th>Message</th></tr></thead><tbody>@forelse($runs as $run)<tr><td>{{ $run->started_at?->format('d/m/Y H:i') }}</td><td>{{ ['discover'=>'Découverte directe','stock'=>'Contrôle stock','taxonomy'=>'Ancien classement','catalog_tree'=>'Lecture arborescence','catalog_crawl'=>'Parcours hiérarchique'][$run->mode] ?? ucfirst($run->mode) }}</td><td><span class="badge {{ $run->status==='success'?'text-bg-success':($run->status==='running'?'text-bg-warning':($run->status==='partial'?'text-bg-warning':'text-bg-danger')) }}">{{ ucfirst($run->status) }}</span></td><td>{{ $run->products_seen }} fiche(s), {{ $run->variants_seen }} variante(s), {{ $run->updated_count }} changement(s)</td><td><span class="text-clamp">{{ $run->message }}</span></td></tr>@empty<tr><td colspan="5">Aucune exécution enregistrée.</td></tr>@endforelse</tbody></table></div></section></div><div class="col-xl-4"><section class="admin-surface h-100"><h2 class="h5 mb-3">Derniers mouvements</h2>@forelse($changes as $change)<div class="supplier-change"><span class="{{ $change->difference >= 0 ? 'text-success' : 'text-danger' }}">{{ $change->difference >= 0 ? '+' : '' }}{{ $change->difference }}</span><div><strong>{{ str($change->supplierProduct?->name)->limit(38) }}</strong><small>{{ $change->old_stock }} → {{ $change->new_stock }} · {{ $change->observed_at?->diffForHumans() }}</small></div></div>@empty<p class="text-secondary mb-0">Aucun changement de stock enregistré.</p>@endforelse</section></div></div>
@endsection
