@extends('layouts.admin') @section('title','Contenus et réglages') @section('admin-content')
<div class="admin-page-heading"><div><small>CONFIGURATION</small><h1>Contenus et informations légales</h1><p>Ces valeurs sont modifiables sans toucher au code.</p></div></div><form method="post" action="{{ route('admin.settings.update') }}">@csrf @method('patch')
@foreach($settings as $group => $items)<section class="admin-surface mb-3"><h2 class="h5 text-capitalize">{{ $group }}</h2><div class="row g-3">@foreach($items as $setting)<div class="{{ $setting->type==='textarea'?'col-12':'col-md-6' }}"><label class="form-label" for="setting-{{ $setting->id }}">{{ str($setting->key)->replace('_',' ')->title() }}</label>@if($setting->type==='textarea')<textarea class="form-control" rows="3" id="setting-{{ $setting->id }}" name="settings[{{ $setting->key }}]">{{ $setting->value }}</textarea>@else<input class="form-control" type="{{ $setting->type==='email'?'email':'text' }}" id="setting-{{ $setting->id }}" name="settings[{{ $setting->key }}]" value="{{ $setting->value }}">@endif</div>@endforeach</div></section>@endforeach<button class="btn btn-dark">Enregistrer les réglages</button></form>
@endsection
