@extends('layouts.admin') @section('title','Produits - Administration') @section('admin-content')
<div class="admin-page-heading"><div><small>CATALOGUE</small><h1>Produits et stocks</h1><p>Gérez le classement, la visibilité et la mise en avant.</p></div><a class="btn btn-dark align-self-start" href="{{ route('admin.products.create') }}"><i class="bi bi-plus-lg me-1"></i> Nouveau produit</a></div>
<form class="admin-filter" method="get"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Nom ou référence"><select class="form-select" name="category"><option value="">Toutes les catégories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->parent ? $category->parent->name.' › '.$category->name : $category->name }}</option>@endforeach</select><select class="form-select" name="status"><option value="">Tous</option><option value="active" @selected(request('status')==='active')>En ligne</option><option value="hidden" @selected(request('status')==='hidden')>Masqués</option><option value="featured" @selected(request('status')==='featured')>Mis en avant</option></select><button class="btn btn-dark">Filtrer</button></form>
<div class="admin-surface table-responsive"><table class="table align-middle"><thead><tr><th>Produit</th><th>Catégorie</th><th>Prix</th><th>Stock</th><th>Visibilité</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr><td><div class="d-flex align-items-center"><img src="{{ $product->image }}" width="54" height="54" style="object-fit:contain" class="me-2" alt=""><div><strong>{{ str($product->name)->limit(52) }}</strong><br><small>{{ $product->sku }} @if($product->featured)· Mis en avant #{{ $product->featured_order }}@endif</small></div></div></td><td>{{ $product->category->name }}</td><td>{{ number_format($product->price,2,',',' ') }} €</td><td><span class="badge {{ $product->stock?'text-bg-success':'text-bg-danger' }}">{{ $product->stock }}</span></td><td>{{ $product->active?'En ligne':'Masqué' }}</td><td><div class="d-flex gap-1"><a class="btn btn-sm btn-outline-dark" href="{{ route('admin.products.edit',$product) }}">Modifier</a><form method="post" action="{{ route('admin.products.delete',$product) }}" onsubmit="return confirm('Archiver ou supprimer ce produit ?')">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">Retirer</button></form></div></td></tr>@endforeach</tbody></table></div><div class="mt-3">{{ $products->links() }}</div>
@endsection
